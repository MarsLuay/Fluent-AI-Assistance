schema_version: tecan.request_spec.v1
request:
  intent: Synthetic stub — second tiny prompt-only script for resolver/path tests.
  verbatim_prompt: Create another prompt-only demo script with one acknowledge step.
  protocol_name: DemoScript_Synthetic_B
  status: reviewed
source:
  contexts: []
  source_scripts: []
generation:
  prompt_only: true
review: {}
acceptance:
  required_checks:
    - Operator acknowledges the second synthetic demo prompt.
verification_recipe:
  worktable: WT_Demo
  labware: []
  groups:
    - name: Operator setup
      description: Synthetic setup group for parser/lint coverage.
      steps:
        - prompt: Acknowledge this second synthetic demo prompt.
          plain_prompt: true
