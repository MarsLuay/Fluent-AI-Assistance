> **Note:** Live FluentControl automation removed; offline helpers only.

# api-v2-078 — Progress.EndInvoke

| Field | Value |
| --- | --- |
| **Method** | `Progress.EndInvoke` |
| **Priority** | low |
| **Workflow** | Offline runtime-report progress policy |

## Improvement

Avoid manual `EndInvoke` pairing in integrators; if async marshaling is required, wrap in a single helper with timeout/cancellation rather than scattering `BeginInvoke`/`EndInvoke` in external check scripts.

## Implementation

- `fluent_pipeline/api_v2/progress_policy.py` — documented alongside api-v2-077; surfaced in `details.api_v2.progress_policy` via `enrich_fluent_context_details()`.

## Usage

Do not call `Progress.EndInvoke` from protocol-builder tooling. Prefer synchronous `ProgressChanged` subscription or bounded helpers (see api-v2-075 pattern for deck-check delegates).
