> **Note:** Live FluentControl automation removed; offline helpers only. Bounded async helper retained for mock runtime tests.

# api-v2-075 — DeckCheckDiscrepancyDetected.EndInvoke

| Field | Value |
| --- | --- |
| **Method** | `DeckCheckDiscrepancyDetected.EndInvoke` |
| **Priority** | low |
| **Workflow** | Offline runtime-report deck-check async policy |

## Improvement

Implement `EndInvoke` only when `BeginInvoke` is used; marshal exceptions and
`IAsyncResult` completion into `runtime_errors` with a bounded wait so incomplete
deck-check async handlers do not leave a long-running offline check stuck.

## Implementation

- `fluent_pipeline/api_v2/events.py` — `wait_deck_check_end_invoke()` bounded helper returns `(ok, reason)` and surfaces exceptions as failures.

## Usage

Call only when api-v2-074 `use_begin_invoke` is explicitly enabled. Default path skips both `BeginInvoke` and `EndInvoke` (`reason: skipped`).
