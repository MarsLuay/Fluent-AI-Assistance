# api-v2-073 — UserPrompt.ToString()

| Field | Value |
| --- | --- |
| **Method** | `UserPrompt.ToString()` |
| **Priority** | medium |
| **Workflow** | prompt_text_quality / prompt_coverage |

## Improvement

Use canonical prompt text (media marker stripped) for `acceptance.required_checks` matching and duplicate detection instead of ad-hoc IR string parsing.

## Implementation

- `fluent_pipeline/api_v2/commands.py` — `UserPrompt.to_string()` strips `[Reference media: …]` before formatting.
- `fluent_pipeline/api_v2/command_summary.py` — `user_prompt_summary()` shared helper.
- `fluent_pipeline/api_v2_user_prompt_validate.py` — prompt_text_quality preflight reuses the same normalized prompt text and placeholder checks.

## Usage

prompt_text_quality keyword overlap and prompt_coverage duplicate/placeholder checks operate on the same normalized operator-visible text FluentControl would show, not the raw IR string with media boilerplate.
