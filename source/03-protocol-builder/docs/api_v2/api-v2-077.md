> **Note:** Live FluentControl automation removed; offline helpers only.

# api-v2-077 — Progress.BeginInvoke

| Field | Value |
| --- | --- |
| **Method** | `Progress.BeginInvoke` |
| **Priority** | low |
| **Workflow** | Offline runtime-report progress policy |

## Improvement

Do not expose or call `BeginInvoke` in workflow tooling; subscribe synchronously to `IRuntimeControllerEvents.ProgressChanged` or poll `GetProgress()` to avoid orphaned async delegate invocations.

## Implementation

- `fluent_pipeline/api_v2/progress_policy.py` — `ProgressSyncPolicy.ALLOW_ASYNC_INVOKE = False` and `progress_wait_guidance()` text recorded in runtime-report details.

## Usage

Protocol-builder tooling must not call `Progress.BeginInvoke`. Use api-v2-071 push events or api-v2-061/062 polling instead.
