# api-v2-072 — TransferLabware.ToString()

| Field | Value |
| --- | --- |
| **Method** | `TransferLabware.ToString()` |
| **Priority** | medium |
| **Workflow** | Post-compile XSCR reinspection |

## Improvement

Normalize `RGA1_TransferLabware` `ExecutionSettings` for roundtrip diffs and log triage instead of comparing hand-escaped XML strings.

## Implementation

- `fluent_pipeline/api_v2/commands.py` — `TransferLabware.to_string()` summarizes labware, destination location/site, and flags.
- `fluent_pipeline/api_v2/command_summary.py` — `transfer_labware_summary()` for IR/log-oriented summaries.
- `fluent_pipeline/api_v2/golden_compare.py` — mismatch findings include `command_summary` via `_command_summary()` (Gate 11 FC-native XML compare).

## Usage

Inspect `fc_native_xml_compare.findings[].command_summary` when Gate 11 reports a transfer mismatch; compare against compiled XSCR without parsing double-escaped XML manually.
