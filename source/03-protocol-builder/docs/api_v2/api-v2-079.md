# api-v2-079 — RemoveLabware.ToString()

| Field | Value |
| --- | --- |
| **Method** | `RemoveLabware.ToString()` |
| **Priority** | low |
| **Workflow** | API V2 `ExecutionChannel` command tracing (deck teardown / simulator parity) |

## Improvement

Log `ToString()` output when emitting `RemoveLabware` via offline stepped validation so `sim_scene` reconciliation shows which labware label was removed without parsing raw XML.

## Implementation

- `fluent_pipeline/api_v2/command_tracing.py` — `format_remove_labware_trace()` emits `RemoveLabware(LabwareName='…')` labels.
- `fluent_pipeline/api_v2/context_enrichment.py` — `log_remove_labware_trace()` appends structured records to `details.command_traces`.

## Usage

When offline stepped runners record deck teardown, call `log_remove_labware_trace(details, labware=…)` so structured traces and `--event-log` show readable removal labels.
