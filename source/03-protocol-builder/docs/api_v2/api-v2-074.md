> **Note:** Live FluentControl automation removed; offline helpers only. Offline event policy only.

# api-v2-074 — DeckCheckDiscrepancyDetected.BeginInvoke

| Field | Value |
| --- | --- |
| **Method** | `DeckCheckDiscrepancyDetected.BeginInvoke` |
| **Priority** | low |
| **Workflow** | Offline runtime-report event policy |

## Improvement

Skip `BeginInvoke` in the default headless offline harness; only use if a custom
provider must stay non-blocking on a UI thread, and pair with `EndInvoke` plus a
timeout so async deck-check callbacks cannot hang a long-running check.

## Implementation

- `fluent_pipeline/api_v2/events.py` — `DeckCheckAsyncPolicy.use_begin_invoke` defaults to `False`.
- `fluent_pipeline/api_v2/runtime.py` — runtime-report details can include the deck-check async policy.

## Usage

Offline CI and generation runs must not scatter `BeginInvoke` for deck-check delegates. Opt in only for UI-thread providers and always pair with api-v2-075.
