# api-v2-080 — RuntimeController.Dispose() [concrete-only]

| Field | Value |
| --- | --- |
| **Method** | `RuntimeController.Dispose()` [concrete-only] |
| **Priority** | low |
| **Workflow** | native API V2 provider lifecycle |

## Improvement

Only member on `RuntimeController` absent from `IRuntimeController`; invoke in provider teardown when holding a concrete instance. Normal path returns `IRuntimeController` from `GetRuntime()`, so this is rarely needed and does not affect offline generate/validate/package.

## Implementation

- `fluent_pipeline/api_v2/runtime_session.py` — `dispose_concrete_runtime_controller()` no-ops on interface-only handles; calls `Dispose()` only when `via_concrete_type=True`.
- `fluent_pipeline/api_v2/runtime_session.py` — lifecycle helpers record concrete-runtime dispose decisions for runtime-report JSON.

## Usage

Prefer `FluentControl.Dispose()` for session cleanup (api-v2-051). Call concrete `RuntimeController.Dispose()` only when the native bridge holds the concrete type explicitly—not the usual `GetRuntime()` interface reference.
